inicio
 
População a[dim]

para i := 0 até i = dim-1
	a[i].fitness := fitness(a[i])
	i := i+1

a[0].fitnessAcumulada := a[0].fitness

para i := 1 até i = dim-1
	a[i].fitnessAcumulada := a[i].fitness + a[i-1].fitnessAcumulada
	i := i+1

totalFitness := a[dim-1].fitnessAcumulada

offset := totalFitness/dim

n := aleatório([0;totalFitness[)

individuo b[0] := procurar(n : a[].fitnessAcumulada)

para i := 1 até i = individuosPretendidos-1
	n := n+offset
	n := n%dim
	Individuo b[i] := procurar(n : a[].fitnessAcumulada)
	i := i+1

retorna População b[]

fim
