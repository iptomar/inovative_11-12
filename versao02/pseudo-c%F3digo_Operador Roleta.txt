inicio
 
População a[dim]

para i := 0 até i = dim-1
	a[i].fitness := fitness(a[i])
	fitnessTotal := fitnessTotal+a[i].fitness
	i := i+1

ordenar_decrescente(a[].fitness)

para i := 0 até i = dim-1
	se i = 0 então
		a[i].probabilidadeAcumulada := a[i].fitness/fitnessTotal
	senão
		a[i].probabilidadeAcumulada := (a[i].fitness/fitnessTotal)+a[i-1].probabilidadeAcumulada
	i := i+1

para i := 0 até i = individuosPretendidos-1
	n := aleatório([0;1[)
	Individuo b[i] := procurar(n : a[].probabilidadeAcumulada)
	i := i+1

retorna População b[]

fim
