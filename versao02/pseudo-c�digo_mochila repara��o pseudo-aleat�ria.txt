inicio

População p[]

para i = 0 até i = tamanho(p[])-1
	p[i] = reparaPseudoAleatorio(p[i].genes[])
	i = i+1

fim




reparaPseudoAleatorio(Individuo genes[])
	pesoMax
	peso[]
	valor[]
	dim = tamanho(genes[])
	para i = 0 até i = dim-1
		se genes[i] = 1 entao
			val = val+valor[i]
			pes = pes+peso[i]
		i = i+1
	se pes <= pesoMax então
		Individuo genes[].fitness = val
		retorna genes[]
	senão
		para i = 0 até i < dim
			relacao[i] = valor[i]/peso[i]
		fazer
			k = k+1
			n = menor(ordem=k : relacao[])
		enquanto (genes[n] = 0)
		genes[n] = 0
		reparaPseudoAleatorio(genes[])
